# Sao chép file này thành .env (cùng thư mục) rồi điền chuỗi kết nối Neon thật.
# File .env đã được .gitignore — KHÔNG commit chuỗi kết nối lên GitHub.

DATABASE_URL=postgresql://<user>:<password>@<host>.aws.neon.tech/<db>?sslmode=require
